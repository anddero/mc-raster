package org.mcraster.reader

//import org.gdal.gdal.gdal


class BinaryTerrainReader {

/*    fun read(file: String) {
        val dataset = gdal.Open(file)

        println("${dataset.rasterXSize} x ${dataset.rasterYSize} = ${dataset.rasterCount}")
    }*/
//    EXAMPLE: BinaryTerrainReader().read("local-data/QGIS_Out/DEM_Test.bt") // unsure whether it works or not

}