package org.mcraster.reader

import java.io.File

class GeoTiffReader {

    fun read(file: String) {
        /*val grid = GeotoolsTiffReader(File(file)).read()

        println("Reading GeoTiff $file")
        println("\tBounds ${grid.envelope2D.bounds2D}")
        println("\tGeometry  ${grid.gridGeometry}")*/

        /*val img = TiffImageParser().getBufferedImage(File(file), null)
        println("Reading GeoTiff $file")
        println("\t${img.width} x ${img.height}")
        println("\t${img.getRGB(0, 0)}")*/
    }
//    EXAMPLE: GeoTiffReader().read("local-data/QGIS_Out/Hillshade_AnneKanal.tif") // fails

}
